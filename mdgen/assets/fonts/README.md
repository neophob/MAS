Lucida Sans assets for the thesis template renderer.

The reference PDF embeds LucidaSans and LucidaSans-Demi. These local assets were taken from the installed Microsoft Office font bundle on the development machine:

- `LucidaSans.ttf`
- `LucidaSansDemi.ttf`

Keep these files available in CI if the pixel-perfect test should render the same typography as the reference PDF.
