❯ npx @openai/codex "see templates - there is a word template called Thesisvorlage mit Titelbild1.0.docx. i want to use markdown and nodejs as runtime to generate my master thesis. so your job is 1) create a workflow to process all markdown files in the doc directory and generate a pdf file. 2) make sure the output looks like the template layout including all the images. keep the generating code in mdgen directory. test the whole process with a dummy file to validate it works as designed"
